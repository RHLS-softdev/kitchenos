Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.6.0
Flask-Cors==4.0.1
python-dotenv==1.0.1
requests==2.32.3
Flask-Limiter==3.8.0

# Desktop sidecar server. Not gunicorn: gunicorn needs fcntl, which doesn't
# exist on Windows, and this now has to run on whatever OS the user's
# machine is. waitress is pure-Python and works the same on all three.
waitress==3.0.1

# Offline dictation (app/voice.py) — replaces the browser's own
# SpeechRecognition API, which was a quiet cloud call (Chrome's
# implementation goes through Google). Model weights are NOT installed by
# this line — see desktop/README.md for downloading + bundling the "small"
# model into the packaged app.
faster-whisper==1.1.0

# psycopg2-binary removed: Postgres is gone from this app entirely now.
# Free tier = local SQLite (see config.py). Cloud/multi-kitchen data lives
# in Convex (/enterprise), not Postgres, so there's nothing left that
# needs a Postgres driver. sentry-sdk removed too — see config.py's
# IS_DESKTOP_FREE_BUILD note; if you ever build a non-desktop variant that
# wants Sentry back, reinstall it then.
